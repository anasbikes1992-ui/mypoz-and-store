# Script archive

One-off diagnose / env helpers **not** wired in `package.json`.

Keep for forensics. Prefer living helpers listed in `docs/WEBXPAY_STAGING_SETUP.md` and `scripts/IMPORT.md`.

Import MCP helpers already live in `scripts/_archive-import/`.
